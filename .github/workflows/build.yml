name: Build and check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Check recovery and preferences
        run: swift test
      - name: Build the app bundle
        run: ./scripts/build-app.sh

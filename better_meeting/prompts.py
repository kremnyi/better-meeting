"""Тексти, що йдуть у артефакти або в модель."""

PROMPT = """Тобі дано матеріали одного робочого мітингу, де людина показувала процес на екрані:

1. `transcript.md` — транскрипція мови з таймкодами. Може містити помилки розпізнавання термінів.
2. `timeline.md` — зшитий таймлайн: мова + тільки НОВІ рядки, що з'являлись на екрані (OCR), + позначки тиші.
   Тиша тут — не порожнє місце: там людина щось робила руками на екрані.
3. `screens/*.jpg` — скріншоти ключових моментів. Таймкод у назві файлу і випалений угорі кадру.
4. `screens_index.md` — відповідність скріншот ↔ таймкод ↔ що на ньому з'явилось.

Задача: скласти **робочу інструкцію (runbook)**, за якою я зможу повторити показане БЕЗ перегляду запису.

Формат — Markdown українською. Команди, шляхи, назви, вивід — англійською, як на екрані.

# <Що саме робимо>

## Передумови
Доступи, інструменти, змінні оточення, гілки/оточення — усе, що треба мати ДО початку.

## Кроки
Нумерований список. Для кожного кроку:
- що робимо і навіщо (1-2 речення)
- точні команди / шляхи / назви у code-блоці
- `[HH:MM:SS]` — таймкод у записі
Не зливай кілька дій в один крок. Порядок = порядок у записі.

## Застереження
Те, що спікер сказав словами: що ламається, що легко пропустити, чого не робити.

## Відкриті питання
Місця, де матеріалів не вистачає, або де OCR/транскрипт явно побиті. Пиши чесно.

Правила:
- Команди й ідентифікатори НЕ переписуй по-своєму — цитуй як на екрані. Якщо на скріншоті і в OCR
  розбіжність — вір скріншоту.
- Якщо на екрані є щось, чого спікер не пояснив, — все одно внеси в кроки і познач `(з екрана, без пояснення)`.
- Якщо чогось немає — пиши "не показано", не додумуй.
"""

HOWTO = """# Що з цим робити

Тека самодостатня. Порядок дій:

1. Відкрий чат з моделлю з великим контекстом (Claude, ChatGPT — будь-яка, що приймає файли).
2. Прикріпи файли:
   - `timeline.md`  ← головний, без нього нічого не вийде
   - `transcript.md`
   - `screens_index.md`
   - усі зображення з `screens/`{sheets_note}
3. Вставь текст із `PROMPT.md` як повідомлення.
4. Отримаєш runbook. Кожну команду перед виконанням звір зі скріншотом по таймкоду —
   OCR плутає `l/1/I` та `0/O`.

## Якщо чат не приймає стільки вкладень

- Підніми поріг: перезапусти з `--max-shots 20`.
- Або склей у сітки: `--sheet 6` (по 6 кадрів на зображення), тоді кидай `sheets/`.
- Мінімальний варіант: `timeline.md` + `PROMPT.md`. Скріншоти дають точність команд, але не критичні.

## Статистика

{stats}
"""

CHUNK_PROMPT = """Це ЧАСТИНА таймлайну запису (частина {i} з {n}). Не роби фінальну інструкцію.
Випиши стисло, зберігаючи таймкоди: виконані дії по порядку, точні команди/шляхи/назви з екрана,
усні застереження, незрозумілі місця. Нічого не вигадуй.
"""

REDUCE_PROMPT = "Нижче — нотатки по частинах одного запису, по порядку. Зший їх в єдиний runbook.\n" + PROMPT
